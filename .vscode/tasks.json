{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build, Test, Doc",
            "type": "shell",
            "command": "nix-shell --pure --run 'NP_BUILD_SKIP_ROOTFS= ./build'",
            "problemMatcher": [ ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
